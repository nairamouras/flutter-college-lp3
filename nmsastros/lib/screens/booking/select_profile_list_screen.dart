import 'package:flutter/material.dart';

class SelectProfileListScreen extends StatelessWidget {
  const SelectProfileListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
