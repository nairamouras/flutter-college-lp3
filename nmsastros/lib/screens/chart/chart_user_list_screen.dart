import 'package:flutter/material.dart';

class ChartUserListScreen extends StatelessWidget {
  const ChartUserListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
